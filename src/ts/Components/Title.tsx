import styled from 'styled-components';

const Title = styled.h1`
  margin-top: 0;
  font-size: 3em;
  line-height: 1em;
  letter-spacing: 2px;
  font-weight: 200;
  padding-right: 0;
  max-width: 600px;
`;

export default Title;
